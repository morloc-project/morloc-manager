<!-- Background context included in the vm-analyst agent session -->
<!-- Edit this file to guide how the analyst consolidates and prioritizes findings -->

## Source code reference

You have read-only access to the morloc compiler source and documentation via
symlinks in the repo root:

- `morloc/` — the full morloc compiler repository (Haskell compiler, Rust
  morloc-manager, build infrastructure, container templates)
- `morloc-project.github.io/` — the morloc documentation site

Use these to validate bug reports, trace root causes to specific code, and
check for discrepancies between documented and actual behavior. NEVER modify
files in either directory.
